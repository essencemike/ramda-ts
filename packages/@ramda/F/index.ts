/**
 * A function that always returns 'false'. Any passed in parameters are ignored.
 *
 * @function
 * @memberof R
 * @since 0.0.1
 * @category Function
 * @sig * -> Boolean
 * @param {*}
 * @return {Boolean}
 * @see R.T
 * @example
 *
 *      R.F(); //=> false
 */
const F = (): boolean => false;
export default F;
